<Window x:Class="MedicalOrganizations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicalOrganizations"
        mc:Ignorable="d"
        Title="Медицинские организации города" Height="450" Width="800">
    <Grid Background="#FF1D1D1D">
        <Grid.RowDefinitions>
            <RowDefinition Height="50px"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="Button1" Grid.Column="0" Content="Больницы" Click="Button1_Click" Style="{StaticResource CustomButtonActiveStyle}"/>
            <Button x:Name="Button2" Grid.Column="1" Content="Доктора" Click="Button2_Click" Style="{StaticResource CustomButtonStyle}"/>
            <Button x:Name="Button3" Grid.Column="2" Content="Пациенты" Click="Button3_Click" Style="{StaticResource CustomButtonStyle}"/>
            <Button x:Name="Button4" Grid.Column="3" Content="Запросы" Click="Button4_Click" Style="{StaticResource CustomButtonStyle}"/>
        </Grid>

        <ContentControl Name="ContentControl" Grid.Row="1" Grid.ColumnSpan="3">
        </ContentControl>
    </Grid>
</Window>
